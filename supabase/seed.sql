-- Demo records require an existing Auth user/profile. Set its UUID only in a local
-- test project, then run scripts/seed-supabase.js. This SQL deliberately contains
-- no credentials or production user identifiers.

